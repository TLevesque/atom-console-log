{
  "name": "atom-console-log",
  "main": "./lib/atom-console-log",
  "version": "0.0.0",
  "description": "On ctrl+alt+a keys press, it inserts console.log('=>${selectedText}', ${selectedText}) to the place of the selected text",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "atom-console-log:convert"
  },
  "repository": "https://github.com/atom/atom-console-log",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
